spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8://testdb
    username: root
    password: rootroot
  jpa:
    hibernate:
      ddl-auto: none

yologram:
  auth:
    jwt:
      secret: X_YOLOGRAM_AUTH_TOKEN
    access-token:
      expire_in_seconds: 10  # 10 seconds